import { FC, useState } from "react";
import { Question } from "../questions";

type Props = {
  questions: Question[];
};

const Quiz: FC<Props> = ({ questions }) => {
  return <div></div>;
};

export default Quiz;
